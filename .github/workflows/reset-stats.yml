name: Reset / Init Stats DB

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '输入 RESET 确认执行（新库=完成初始化建表，旧库=清空历史数据，此操作不可撤销）'
        required: true

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - name: Verify confirmation
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "RESET" ]; then
            echo "confirmation text mismatch, aborting" >&2
            exit 1
          fi

      - name: Ensure schema and clear rows
        env:
          TURSO_URL: ${{ secrets.TURSO_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        run: |
          set -e
          url="$(echo "$TURSO_URL" | sed -e 's#^libsql://#https://#' -e 's#/*$##')/v2/pipeline"
          curl -sf -X POST "$url" \
            -H "Authorization: Bearer $TURSO_AUTH_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"requests":[
              {"type":"execute","stmt":{"sql":"CREATE TABLE IF NOT EXISTS translation_stats (id INTEGER PRIMARY KEY AUTOINCREMENT, count INTEGER NOT NULL, created_at INTEGER NOT NULL)"}},
              {"type":"execute","stmt":{"sql":"CREATE INDEX IF NOT EXISTS idx_translation_stats_created_at ON translation_stats(created_at)"}},
              {"type":"execute","stmt":{"sql":"DELETE FROM translation_stats"}},
              {"type":"close"}
            ]}'
          echo "translation_stats ready and cleared"
